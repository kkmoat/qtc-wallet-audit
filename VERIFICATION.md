# 本次验证记录

日期：2026-09-11。市场源码版本：`56b5d98a4797ff7168144ecf0e138f2273ae0c05`。本地测试及本次线上钱包资产比对已完成。

| 检查 | 本次结果 | 范围 |
| --- | --- | --- |
| 原样来源/哈希 | 36 个复制或提取文件匹配 | 2026-09-11 06:11 UTC，逐项读取上述精确 Git 提交中的源文件或 ZIP 成员，确认字节、长度和 SHA-256 与清单相同；源清单不包含整个市场。 |
| 既有钱包回归 | 43 项检查通过 | 真实 Worker/WASM、Node 浏览器模拟层、内存中临时钱包。 |
| 随机输入与失败处理 | 8 个子用例及外层测试通过（Node 报告 9 tests） | 32/16/12 字节调用、不同创建、相同恢复、CSPRNG 缺失及熵/盐/IV 各自抛错拒绝、无弱随机回退、意外 fetch 拒绝。 |
| 线上资产比对 | 8 个 `/crypto/` 文件全部 MATCH | `https://uniqtc.xyz`，2026-09-11 06:14:38 UTC。包括本次更新的 `domain.mjs`，仅下载和 SHA-256 比较，不执行远程代码；不验证整站或其他时间、用户收到的内容。 |
| 更新目录范围 | 仅刷新清单中已有的 4 个源文件和对应哈希 | 文件总范围仍为 46 个；没有加入后台、数据库、成交记录、安全报告或生产配置。此前有限凭据模式扫描不是无遗漏保证，本次不冒充已重新完整扫描。 |
| 包内依赖完整性 | 源码 ZIP 字节未变，沿用此前完整性检查记录 | 此前 50 个 vendor 包、1,712 个文件与 `.cargo-checksum.json` 匹配；本次未重新构建或逐个重验依赖，也不代表依赖没有漏洞。 |

本次在已有范围内更新 `lib/market/client.mjs`、`lib/market/domain.mjs`、`public/crypto/domain.mjs` 和 `lib/i18n-messages.mjs`，同步 `history_add` 操作的签名字段、审核密码请求类型及文案。钱包 Worker、加密实现、WASM 和 Rust 源码 ZIP 字节未变。2026-09-11 06:08 UTC 重新运行 `npm test`，36 个清单文件、43 项钱包回归检查、9 个随机性测试均通过；这些测试不涵盖补录成交的完整服务器流程。

源代码核查追踪了创建 → 备份 UI → 本地保存 → 恢复 → 签名 → 网络请求。发现并在 README 中明确记录：创建结果会返回助记词给本地 UI、复制会写系统剪贴板、管理员审核密码会单独上传、导入 JSON 额外字段不会自动移除。

本次没有使用真实钱包，没有提交真实订单或转账，没有修改网站，没有读取生产数据库中的私密记录。Rust/WASM 未在本次重新构建，也未运行独立官方钱包的固定兼容性向量；未执行完整浏览器/UI/部署/服务器独立安全审计。测试通过不能证明网站过去和未来的全部行为。GitHub CI 只有在实际运行成功后才构成该版本的 CI 验证；此记录不冒充已完成的远程 CI。

可复查命令在 [README](README.md)。更新文件后请重新生成来源清单和执行验证，不要沿用旧结果作为新版本证明。
