# 本次验证记录

日期：2026-09-11。市场源码版本：`eb9535f01862c07beec047d8e0e14d79c164327e`。选中源码和源码 ZIP 成员已核对该精确 Git 提交；本地测试和本次线上钱包资产比对已完成。

| 检查 | 本次结果 | 范围 |
| --- | --- | --- |
| 原样来源/哈希 | 36 个文件匹配精确市场提交 | 2026-09-11 07:48 UTC，从上述提交读取 26 个直接复制文件和 10 个源码 ZIP 成员，逐项核对内容、长度和 SHA-256；对应 Git tree 为 `f16b5d69f2a73d631396e212b115b868ada51e4c`。清单不包含整个市场。 |
| 钱包回归与启动处理 | 65 项检查通过 | 2026-09-11 07:27 UTC 完成 `npm test`。真实 Worker/WASM、Node 浏览器模拟层、内存中临时钱包；包括加载错误、实际 15 秒中止、排队操作和处理阶段消息检查。 |
| 随机输入与失败处理 | 8 个子用例及外层测试通过（Node 报告 9 tests） | 同次 `npm test`；既有随机性测试已适配独立加载握手和阶段消息，覆盖 32/16/12 字节调用、不同创建、相同恢复、CSPRNG 失败、无弱随机回退及意外 fetch 拒绝。 |
| 线上资产比对 | 8 个 `/crypto/` 文件全部 MATCH | `https://uniqtc.xyz`，2026-09-11 07:52:33.971 UTC。包括本次更新的 `wallet-worker.js`；仅下载、比较长度及 SHA-256，不执行远程代码。结果不覆盖页面 HTML、UI 构建文件、服务器处理、其他时间或所有用户收到的内容。 |
| 更新目录范围 | 仅刷新已有选中文件、现有测试和说明 | 文件总范围仍为 46 个；没有加入市场工作台、后台、数据库、成交记录、安全报告或生产配置。此前有限凭据模式扫描不是无遗漏保证，本次不冒充已重新完整扫描。 |
| 包内依赖完整性 | 源码 ZIP 字节未变，沿用此前完整性检查记录 | 此前 50 个 vendor 包、1,712 个文件与 `.cargo-checksum.json` 匹配；本次未重新构建或逐个重验依赖，也不代表依赖没有漏洞。 |

本次原样同步 `lib/market/wallet.ts`、`lib/market/use-wallet.ts`、`components/market/wallet-dialog.tsx`、`public/crypto/wallet-worker.js`、`lib/i18n-messages.mjs` 和两个既有 Worker 回归测试文件。现有公共随机性测试仅调整加载握手与阶段消息处理。加密实现、WASM、签名协议、网络请求实现和 Rust 源码 ZIP 字节未变。当前测试不执行客户端/React 生命周期、市场工作台或完整服务器流程。

源代码核查追踪了创建 → 备份 UI → 本地保存 → 恢复 → 签名 → 网络请求。发现并在 README 中明确记录：创建结果会返回助记词给本地 UI、复制会写系统剪贴板、管理员审核密码会单独上传、导入 JSON 额外字段不会自动移除。

本次快照整理没有使用真实钱包，没有提交真实订单或转账，没有修改网站，没有读取生产数据库中的私密记录。Rust/WASM 未在本次重新构建，也未运行独立官方钱包的固定兼容性向量；未在本公共快照中执行完整浏览器/UI/部署/服务器独立安全审计。测试通过不能证明网站过去和未来的全部行为。GitHub CI 只有在实际运行成功后才构成该版本的 CI 验证；此记录不冒充已完成的远程 CI。

可复查命令在 [README](README.md)。更新文件后请重新生成来源清单和执行验证，不要沿用旧结果作为新版本证明。
