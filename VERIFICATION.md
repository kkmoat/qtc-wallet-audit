# 本次验证记录

日期：2026-09-11。市场源码版本：`7865c310c8d8cdcba88f700052b85745806a0519`。选中源码和源码 ZIP 成员已核对该精确 Git 提交；本地完整性、钱包测试和本次线上钱包资产比对已完成。

| 检查 | 本次结果 | 范围 |
| --- | --- | --- |
| 原样来源/哈希 | 36 个文件匹配精确市场提交 | 2026-09-11 10:21:31 UTC，从上述提交读取 26 个直接复制文件和 10 个源码 ZIP 成员，逐项核对内容、长度和 SHA-256；对应 Git tree 为 `7dba92b10fe61391b6465a5706a8e99a6ea0a6f0`。清单不包含整个市场。 |
| 钱包回归与启动处理 | 65 项检查通过 | 2026-09-11 10:17 UTC 完成 `npm test`，同时验证清单内 36 个文件的长度与 SHA-256。使用真实 Worker/WASM、Node 浏览器模拟层、内存中临时钱包；包括加载错误、实际 15 秒中止、排队操作和处理阶段消息检查。 |
| 随机输入与失败处理 | 8 个子用例及外层测试通过（Node 报告 9 tests） | 同次 `npm test`；既有随机性测试覆盖 32/16/12 字节调用、不同创建、相同恢复、CSPRNG 失败、无弱随机回退及意外 fetch 拒绝。 |
| 线上资产比对 | 8 个公开钱包文件全部 MATCH | 2026-09-11 10:23:23.939 UTC，执行 `npm run verify:site -- https://uniqtc.xyz/`，逐项匹配当前快照的 `/crypto/` 文件长度与 SHA-256。该比对不包含本次变更的 `lib/market/client.mjs` 或 UI 构建文件，也不验证 HTML、浏览器扩展、服务端处理、过去/未来部署或每位访客的响应。 |
| 更新目录范围 | 仅刷新已有 2 个选中源文件、清单和两份说明，共 5 个文件 | 文件总范围仍为 46 个；没有加入市场工作台、管理员后台或私有测试、数据库、成交记录、安全报告或生产配置。此前有限凭据模式扫描不是无遗漏保证，本次不冒充已重新完整扫描。 |
| 包内依赖完整性 | 源码 ZIP 字节未变，沿用此前完整性检查记录 | 此前 50 个 vendor 包、1,712 个文件与 `.cargo-checksum.json` 匹配；本次未重新构建或逐个重验依赖，也不代表依赖没有漏洞。 |

本次原样同步 `lib/market/client.mjs` 和 `lib/i18n-messages.mjs`。市场 API 客户端仅对四类审核动作的固定同源提交启用会话 Cookie；通用请求、签名挑战及普通操作继续省略 Cookie，旧显式 `reviewPassword` 提交仍兼容且不进入签名载荷。HTTP 401 交给调用界面处理，客户端不自动重放。钱包 Worker、加密实现、WASM、签名协议、既有测试和 Rust 源码 ZIP 字节未变，没有新增密钥处理。后台访问密码仍由登录请求发送服务器验证，但该登录/重登界面、Cookie 签发及服务端审核授权不在公共包内；当前公共测试不执行此 API 客户端、完整 React 生命周期或上述服务器流程。

源代码核查追踪了创建 → 备份 UI → 本地保存 → 恢复 → 签名 → 网络请求。README 明确记录：创建结果会返回助记词给本地 UI、复制会写系统剪贴板、后台访问密码以及兼容旧客户端的显式审核密码由服务器验证，用户本地钱包助记词和解锁密码不属于这些请求字段；导入 JSON 额外字段不会自动移除。

本次快照整理没有使用真实钱包，没有提交真实订单或转账，没有修改网站，没有读取生产数据库中的私密记录。Rust/WASM 未在本次重新构建，也未运行独立官方钱包的固定兼容性向量；未在本公共快照中执行完整浏览器/UI/部署/服务器独立安全审计。测试通过不能证明网站过去和未来的全部行为。GitHub CI 只有在实际运行成功后才构成该版本的 CI 验证；此记录不冒充已完成的远程 CI。

可复查命令在 [README](README.md)。更新文件后请重新生成来源清单和执行验证，不要沿用旧结果作为新版本证明。
